<div class="container content-container">
  <h1>Ache a unidade carrefour mais próximo de vocês</h1>
  <p class="subtitle">Usando os ceps que você entrou no applicativo, fizemos uma classificaçao das lojas carrefour mais proximas de vocês.</p>
    
  <%= simple_form_for @trade, url: wizard_path do |f| %>
    <%= f.input :carrefour_unit_id, label: "Unidade do Carrefour:", as: :select, collection: @carrefour_units ? @carrefour_units : CarrefourUnit.all, hint: "As unidades aparecem na ordem de proximidade entre você e o #{ params[:author_role] == "Vendedor" ? "comprador" : "vendedor"}." %>
    <div id="map"
      style="width: 100%; height: 400px;"
      data-markers="<%= @markers.to_json %>"
      data-mapbox-api-key="<%= Rails.application.credentials.mapbox[:api] %>">
    </div>
    <div class="d-flex justify-content-between">
      <%= link_to 'Voltar', trade_step_path(@trade, "location"), class: "btn btn-outline-primary" %>
      <%= f.submit "Avançar", class: "btn btn-primary" %>
    </div>
  <% end %>
</div>