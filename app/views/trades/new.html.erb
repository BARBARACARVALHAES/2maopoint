<%= simple_form_for(@trade, url: trades_path) do |f| %>
  <%= f.input :author, lable: "Nesta transação, você é o:", as: :radio_buttons, collection: ["Vendedor", "Comprador"] %>
  <%= f.association :item_category, as: :select, label: "A categoria do item é:", collection: @item_categories %>
  <%= f.input :item, label: "O item transacionado é:" %>
  <%= f.submit "Avançar" %>
<% end %>
